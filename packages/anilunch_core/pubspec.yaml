name: anilunch_core
description: Shared pure-Dart client for the AniLunch Go API (REST + WebSocket + sync).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  dio: ^5.4.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
